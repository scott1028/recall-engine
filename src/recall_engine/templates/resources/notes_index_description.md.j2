List Markdown notes in the selected recall-engine knowledge repo as a JSON array. Each item includes `path`, `relative_path`, and `resource_uri` for reading the note. Use `relative_path` and `path` as semantic hints when selecting a note.

Discovery rules:
{% include "rules/multilingual_keyword_expansion.md" %}
